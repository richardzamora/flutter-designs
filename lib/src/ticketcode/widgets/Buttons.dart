import 'package:flutter/material.dart';

class SocialNetworkRoundedButton extends StatelessWidget {
  Widget child;
  Color backgroundColor;
  double borderRadious;
  SocialNetworkRoundedButton(
      {Key? key,
      required this.child,
      this.backgroundColor = Colors.white,
      this.borderRadious = 10})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
